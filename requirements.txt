# ===== Framework web =====
fastapi>=0.115
uvicorn[standard]>=0.32

# ===== Validação / Settings =====
pydantic[email]>=2.9            # extra `email` instala o `email-validator`, exigido por EmailStr
pydantic-settings>=2.6

# ===== Banco de dados =====
sqlalchemy>=2.0
psycopg[binary]>=3.2
alembic>=1.13

# ===== Supabase =====
supabase>=2.9

# ===== Autenticação / JWT =====
pyjwt[crypto]>=2.9              # extra `crypto` instala `cryptography`, necessário para ES256/RS256

# ===== Utilitários =====
python-multipart>=0.0.12       # upload de arquivos
python-dotenv>=1.0
httpx>=0.27

# ===== Exportação de relatórios =====
openpyxl>=3.1
fpdf2>=2.8

# ===== IA — extração de texto de documentos (Fase 1) =====
pypdf>=5.0                     # texto de PDFs
python-docx>=1.1               # texto de arquivos Word (.docx)

# ===== IA — provedores reais (instale apenas o que for usar) =====
anthropic>=0.40                # provedor Claude (AI_PROVIDER=anthropic)
openai>=1.40                   # provedor OpenAI (AI_PROVIDER=openai)
# OCR (imagens / PDFs escaneados) — necessário apenas no provedor LOCAL:
#   pytesseract>=0.3            # + binário do Tesseract instalado no SO
